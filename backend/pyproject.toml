[project]
name = "procuregpt-backend"
version = "1.0.0"
description = "AI Procurement Automation System - Backend"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 100
